<Page
    x:Class="HuaCards.Views.BrowsePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="#FFF2E1"/>
        <Style x:Key="HeaderTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="56"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource HeaderBackgroundBrush}">
            <TextBlock Style="{StaticResource HeaderTitleStyle}" Text="Your vocabulary"/>
        </Border>

        <ScrollViewer Grid.Row="1">
            <StackPanel MaxWidth="900" HorizontalAlignment="Center" Spacing="12" Padding="16,20">
                <!-- Replace with your real list binding. This keeps the page readable for now. -->
                <TextBlock Text="Vocabulary list will appear here." Foreground="#666"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>

